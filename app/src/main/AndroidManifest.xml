<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cocktailmaker">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBkMsjQf8f_3H2Ui57WfMi7Y3tl0NF6jEk" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps"></activity>
        <activity android:name=".Dark_Stormy_Activity" />
        <activity android:name=".Mojito_Activity" />
        <activity android:name=".Brample_Gin" />
        <activity android:name=".Screw_Driver" />
        <activity android:name=".Moscow_Mule" />
        <activity android:name=".Hot_Toddy" />
        <activity android:name=".Tequila_Sunrise" />
        <activity android:name=".Tequila_Highball" />
        <activity android:name=".Pina_Colada" />
        <activity android:name=".English_Garden" />
        <activity android:name=".layoutPage" />
        <activity android:name=".drink2" />
        <activity android:name=".drink3" />
        <activity android:name=".drink4" />
        <activity android:name=".drink5" />
        <activity android:name=".drink6" />
        <activity android:name=".drink7" />
        <activity android:name=".drink8" />
        <activity android:name=".drink9" />
        <activity android:name=".drinkone" />
        <activity
            android:name=".Whiskey_Sour_Activity"
            android:label="@string/title_activity_whiskey__sour_"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Activity_2" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>